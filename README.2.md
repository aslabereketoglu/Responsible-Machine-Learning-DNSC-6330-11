
Question 1 Compute SHAP values for your model on the test set. Produce: (a) a beeswarm summary plot, (b) a waterfall plot for the highest-risk and lowest-risk defendant in each racial group.

Code Explanation: In this step, SHAP (Shapley Additive Explanations) is applied to the trained gradient-boosted tree model to interpret its predictions on the test set. First, the test data is transformed using the same preprocessing pipeline to ensure consistency with the model’s inputs. A SHAP explainer is then created for the trained model, and SHAP values are computed for all observations in the test set. These values represent the contribution of each feature to an individual prediction relative to the average prediction.

For part (a), a bee-swarm summary plot is generated using all SHAP values. This plot provides a global view of feature importance by showing how each feature influences predictions across the entire test set. Features are ranked by importance, and the distribution of SHAP values indicates both the magnitude and direction of their impact on the model’s output.

For part (b), the analysis focuses on individual-level explanations. The predicted probabilities are computed for all test observations, and within each racial group, the individuals with the highest and lowest predicted risk are identified. For these selected cases, waterfall plots are generated using SHAP values. Each waterfall plot breaks down a single prediction into additive feature contributions, showing how specific features push the prediction higher or lower relative to the baseline.

Overall, this step combines global and local interpretability: the bee-swarm plot explains overall model behavior, while the waterfall plots explain individual decisions, allowing for comparison of model reasoning across different racial groups.

Result:

The SHAP results suggest that the model’s predictions are primarily driven by a small set of dominant features, especially priors_count and age-related variables, which consistently have the largest impact across individuals and groups. The beeswarm plot shows that higher priors strongly push predictions toward higher risk, while age categories also systematically influence outcomes. The waterfall plots reinforce this at the individual level: high-risk predictions are typically driven by large positive contributions from priors_count, whereas low-risk predictions result from negative contributions of the same feature and related variables. While this consistency indicates that the model is relying on meaningful signals, the presence of race-related features, even with smaller contributions, auggests potential proxy effects or indirect bias. Additionally, the magnitude of contributions varies significantly across individuals, indicating that similar outcomes can be driven by different combinations of features. Overall, the results imply that the model is heavily sensitive to criminal history and demographic factors, and although it appears structurally consistent, it still raises governance concerns regarding fairness, feature reliance, and the potential for unequal impact across groups.

 Question 2 Run LIME on the same four individuals. Compare LIME and SHAP feature attributions. Where do they agree? Where do they diverge? What does divergence imply for governance?

Code Explanation : In this step, I applied LIME to the same four test-set individuals used in the SHAP analysis to compare two different local explanation methods for the same model predictions. First, I transformed the training and test sets using the fitted preprocessor so that LIME operates on the same encoded feature space as the gradient-boosted tree. I then created a LimeTabularExplainer using the encoded training data, along with the feature names and class labels. Next, I computed predicted probabilities on the test set and selected the four individuals of interest: the highest-risk and lowest-risk African-American defendants and the highest-risk and lowest-risk Caucasian defendants. For each selected case, I generated a LIME explanation by fitting a local surrogate model around that observation and extracting the top contributing features. I also retrieved the corresponding SHAP values for the same individuals, ranked the top features by their absolute contributions, and printed both LIME and SHAP explanations side by side. Finally, I stored the results in data frames and merged them into a comparison table, allowing me to directly observe where the two methods agree on important features and where they differ. This process enables me to assess the consistency and reliability of explanations, which is important for model transparency and governance.

Results: 
These results show that LIME and SHAP generally agree on the main drivers of predictions, but differ in how strongly and consistently they attribute importance to features. Across all four individuals, both methods consistently identify priors_count as the most influential feature—higher prior offenses strongly increase predicted risk, while lower values reduce it. Age-related features also appear in both methods, with younger age (e.g., “Less than 25”) pushing risk up and older age pushing it down. This agreement suggests that the model’s core behavior is stable and driven by intuitive, domain-relevant factors.

However, there are clear divergences. SHAP assigns much larger and more consistent magnitudes (e.g., very large positive contributions from priors_count), while LIME distributes weights more evenly and sometimes highlights additional features such as race indicators or categorical splits. In some cases, LIME includes features that SHAP does not rank among the top contributors, and vice versa. This reflects the methodological difference: SHAP provides a globally consistent, additive decomposition, while LIME approximates the model locally with a linear surrogate, making it more sensitive to small perturbations in the data.

The comparison table further shows that many features appear in only one method (with zeros in the other), reinforcing that explanations are not unique or absolute.

From a governance perspective, this divergence is important: it implies that no single explanation method should be trusted in isolation. Agreement increases confidence in key drivers, but disagreement signals potential instability, proxy effects, or model complexity that is not fully captured by one method. Therefore, these results support the lecture principle that transparency is diagnostic, not cosmetic—differences between LIME and SHAP should be investigated as potential risks rather than ignored.

Question 3: Generate at least one counterfactual per individual using DiCE (Python) or NICE (R). Report the minimal feature changes required to flip the prediction. Flag any counterfactuals that require changes to immutable features (race, sex)

Code Explanation: In this step, I used DiCE to generate counterfactual explanations for the same four individuals selected earlier: the highest-risk and lowest-risk African-American defendants and the highest-risk and lowest-risk Caucasian defendants from the test set. First, I prepared the training data in the format required by DiCE by combining the feature set and target variable, then defined the continuous features and outcome column. Next, I wrapped the trained gradient-boosted tree pipeline as a DiCE model so that counterfactuals could be generated directly from the fitted classifier. I then identified the four selected individuals using predicted probabilities from the test set and, for each one, asked DiCE to generate up to three counterfactuals with the opposite predicted class. After that, I extracted the first available counterfactual and compared it to the original observation feature by feature in order to identify the minimal changes required to flip the prediction. I also explicitly checked whether any of the changed features were immutable attributes—race_factor or gender_factor—and flagged them if so. Finally, I stored a summary of the results in a table showing each individual’s race group, risk level, the set of changed features, and whether any immutable features had to be altered. This step allows me to evaluate whether the model offers actionable recourse or whether changing protected characteristics would be necessary to alter outcomes.

Result Explanation: These results show that the counterfactual explanations are often not actionable and raise fairness concerns. For the highest-risk African-American and Caucasian individuals, the minimal changes required to flip the prediction frequently involve changing race, along with features like priors count. Similarly, for the lowest-risk African-American case, the model suggests that changing race (e.g., African-American Caucasian) would flip the outcome. Because race is an immutable and protected attribute, these counterfactuals are flagged as problematic and indicate that the model’s decision boundary is sensitive to race or to variables strongly correlated with it.

In contrast, only one case—the lowest-risk Caucasian individual—produced a counterfactual that did not require changing immutable features. In that case, the prediction could be flipped by modifying age category and priors count, which are at least conceptually actionable (even if not fully realistic).

Overall, these results imply that while the model sometimes provides actionable recourse, it often relies on protected attributes or their proxies to determine outcomes. From a governance perspective, this is a serious issue: if changing race is part of the “minimal” path to a different decision, it suggests potential bias or indirect discrimination in the model. This aligns with the lecture point that counterfactuals should be actionable and fair, and when they are not, they reveal deeper problems in the model rather than providing meaningful explanations.

Question 4 Write a 300-word governance memo addressed to a hypothetical court auditor: summarise what your explanations reveal about model behaviour, any limitations of the methods, and what further monitoring you recommend.

Question 4 :
This memo summarizes findings from applying SHAP, LIME, and counterfactual explanations to the recidivism model. Across methods, the model consistently relies on features such as prior offenses (priors_count) and age, indicating that these are primary drivers of predictions. SHAP provides globally consistent attributions, showing that higher priors strongly increase predicted risk, while younger age groups also contribute to higher risk scores. LIME, as a local method, generally agrees on these key drivers but occasionally highlights additional features such as race-related indicators, suggesting sensitivity to local data structure and potential instability. Counterfactual analysis shows that predictions can often be flipped through relatively small changes in priors_count or age category, and importantly, no counterfactual required changing immutable attributes such as race or sex, indicating some level of actionable recourse.

However, transparency does not imply trust. While the explanations improve understanding, they also reveal potential concerns. The presence of race-related features in local explanations suggests possible proxy effects or indirect bias, even if not dominant globally. Additionally, LIME and SHAP occasionally diverge, reflecting that both are approximations of the model rather than ground truth. Counterfactuals, while useful, may not always represent realistic or feasible interventions.

No single method is sufficient: SHAP explains overall feature influence and provides a theoretically grounded, model-agnostic decomposition of predictions that satisfies key fairness properties and ensures full attribution. LIME captures local behavior through interpretable surrogate models, while counterfactuals focus on actionable recourse by identifying minimal changes needed to alter outcomes. Their combined use highlights both consistent patterns and areas of concern. Given these findings, the recommended ongoing monitoring that includes regular fairness audits (e.g., error rates across demographic groups), stability checks of explanations, and review of potential proxy variables. Transparency must be embedded in governance processes, including documented audits and clear recourse mechanisms, to ensure that model behavior remains aligned with legal and ethical standards.









